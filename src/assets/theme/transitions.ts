import { TransitionsOptions } from '@mui/material';

export const transitions: TransitionsOptions = {
  // easing: 'easeIn',
  duration: {
    // standard: 3000, //! afecta a varios componentes como las Tabs y hace que se vaen lentas
  },
};
